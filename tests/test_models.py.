    def test_delete_a_product(self):
        """It should Delete a Product"""
        product = ProductFactory()
        product.create()
        self.assertEqual(len(Product.all()), 1)
        # delete the product and check that it have deleted
        product.delete()
        self.assertEqual(len(Product.all()), 0)
